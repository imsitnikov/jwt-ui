/// <reference types="node" />
import type { Server } from 'http';
import type { AppConfig, NodeKit } from '@gravity-ui/nodekit';
import { Express } from 'express';
import type { AppRoutes } from './types';
export declare class ExpressKit {
    nodekit: NodeKit;
    config: AppConfig;
    express: Express;
    httpServer?: Server;
    constructor(nodekit: NodeKit, routes: AppRoutes, hooks: {
        beforeParsers?: (express: Express) => void
        beforeRoutes?: (express: Express) => void
    });
    run(): void;
    private getAppSocket;
    private getListenTarget;
}
